services:
  backend:
    build: .
    container_name: stain-backend
    ports:
      - "8081:8081"
    env_file:
      - .env
    volumes:
      - ./models:/app/models
      - ./.env:/app/.env
      - ./app:/app/app
    restart: unless-stopped